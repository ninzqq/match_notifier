/// A placeholder class that represents an entity or model.
class TeamItem {
  const TeamItem(this.name, this.address, this.icon);

  final String name;
  final String address;
  final String icon;
}
